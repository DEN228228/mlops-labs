name: Model CI (Train, Test, Report)

on:
  pull_request:

permissions:
  contents: read
  pull-requests: write

jobs:
  ci:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: "3.11"

      - name: Install uv
        uses: astral-sh/setup-uv@v3

      - name: Install dependencies via uv
        run: uv sync

      - name: Lint (flake8)
        run: uv run flake8 . --exclude=.venv,venv,mlruns,data --count --select=E9,F63,F7,F82 --show-source --statistics
        
      - name: Format check (black)
        run: uv run black . --check

      - name: Run DVC pipeline (Download, Prepare, Test Data, Train, Test Model)
        env:
          R2_THRESHOLD: "0.70"
        run: |
          uv run dvc repro

      - name: Setup CML
        if: github.event_name == 'pull_request'
        uses: iterative/setup-cml@v2

      - name: Create CML report (PR only)
        if: github.event_name == 'pull_request'
        env:
          REPO_TOKEN: ${{ secrets.GITHUB_TOKEN }}
          R2_THRESHOLD: "0.70"
        run: |
          cat <<EOF > report.md
          ## 📊 Результати навчання моделі
          
          **Метрики (metrics.json):**
          \`\`\`json
          $(cat metrics.json)
          \`\`\`
          
          **Графік (Actual vs Predicted):**
          $(cml publish plots/best_model_actual_vs_predicted.png --md)
          
          ###Quality Gate
          Мінімальний поріг R2: **$R2_THRESHOLD**
          EOF
          
          cml comment create report.md

      - name: Upload model artifact (main only)
        if: github.ref == 'refs/heads/main' && github.event_name == 'push'
        uses: actions/upload-artifact@v4
        with:
          name: production-model
          path: models/